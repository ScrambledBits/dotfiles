# mise configuration - version manager for multiple tools
# https://mise.jdx.dev/
# Usage: mise install (installs all tools listed here)

[tools]
# Python for teaching and automation
python = "3.12"
poetry = "1.7"
ruff = "0.4.10"      # Fixed: specific patch version

# Node.js for slidev and modern tools
node = "20"

# Go for cloud-native tools
go = "1.22"

# Terraform ecosystem
terraform = "1.14.7"  # Latest stable
terragrunt = "0.55.21" # Fixed: specific patch version
tflint = "0.50.3"

# Cloud CLIs
"pipx:awscli" = "latest"

[settings]
# Automatically install missing tools when entering directories
experimental = true

[env]
# Global environment variables
PYTHONDONTWRITEBYTECODE = "1"
PIP_REQUIRE_VIRTUALENV = "true"

[tool_alias]
# Short names for common tools
tf = "terraform"
tg = "terragrunt"
