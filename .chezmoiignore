# Files to exclude from chezmoi management
# https://www.chezmoi.io/reference/special-files/chezmoiignore/

# Machine-local secret overrides (create manually, never commit)
.chezmoidata/local.toml
.chezmoidata/secrets.yml

# Local shell overrides
.zshrc.local

# Claude runtime files — settings.json excluded so machine-specific hooks are preserved
.claude/settings.json
.claude/backups
.claude/cache
.claude/downloads
.claude/file-history
.claude/ide
.claude/mcp-needs-auth-cache.json
.claude/policy-limits.json
.claude/plugins
.claude/projects
.claude/remote-settings.json
.claude/session-env
.claude/sessions
.claude/shell-snapshots
.claude/history.jsonl

# VS Code runtime files (keep argv.json only)
.vscode/extensions
.vscode/cli

# Brewfiles are source-side inputs for run_onchange scripts, not deployed dotfiles
Brewfile
Brewfile.MacOS

# Ghostty: deploy macOS config only on macOS, Linux config only on Linux
{{ if ne .chezmoi.os "darwin" }}
Library/Application Support/com.mitchellh.ghostty/config.ghostty
{{ end }}

{{ if ne .chezmoi.os "linux" }}
.config/ghostty/config.ghostty
{{ end }}
